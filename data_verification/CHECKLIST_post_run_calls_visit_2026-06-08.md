---
# Чек-лист пост-прогонной верификации: визит-сторона (Var A) + звонки→посевы

> Дата: 2026-06-08. Согласовано с пользователем. Выполнять ПОСЛЕ применения обоих фиксов на чистом состоянии БД Victory.
> БД-проверки — `anton_sql`; ре-ран/правки — `director`; главной сессии скрипты big_analytics_v5 запрещены (хук).

## Два фикса
1. **Var A (визит-сторона, `step13_arrival/step13.py`)**: убран хардкод `направление='Контекст'` в Авто-ветках → направление считается логикой step3 (UTM/статус); добавлена пиксель-proxy ветка (`направление='пиксель'`, Date=дата заявки). Цель: визит-сторона покрывает SEO/SEO Flow/пиксель, Контекст де-раздувается (июнь 73→~68 + SEO 5).
2. **Звонки→посевы (`step6_build_full/step6.py:415–443`, UPDATE 3c)**: посевы звонку ТОЛЬКО если домен засеян в ТОТ ЖЕ месяц (crop-строка того же месяца + `kol_vo_zayavok>0`); убрано «заражение по домену» и ветка B. Цель: ложные 116 дом/3015 обр/39 продаж → ~0 (в Контекст/SEO по статусу); by-design 7 дом/304 обр/2 продажи сохранены.

## Принцип
Ничего не теряем и не двоим. Заявка-сторона — суммы ИНВАРИАНТНЫ. Визит-сторона — суммы НАМЕРЕННО РАСТУТ (Var A добавляет SEO/пиксель): «не потерять» = старое не пропало + прирост = именно SEO/SEO Flow/пиксель.

## Чек-лист
### 0. Гейты
- [ ] Var A pipeline `Completed`; фикс звонков применён, ре-ран `Completed`, таблицы синхронны.

### 1. GOLDEN Кудерко — жёсткий гейт (anton_sql)
- [ ] Расход = 25 422 774.00, продажи = 47 (специалист «Кудерко Семен», «По дате заявки», источники direct/tp8/seo/calls/direct_unmatched/direct_zero, **БЕЗ фильтра по `"Date"`**, без пикселя, до копейки). Обращения/квалы/визиты — дрейф ±единицы. Поехало → ОТКАТ. _Жёсткий инвариант — единый источник [`../GOLDEN_BASELINE.md`](../GOLDEN_BASELINE.md). (Старый вид «25 422 774.03 / диапазон 01.01–04.06» устарел: расход дрейфанул .03→.00 после пересборок 10–11.06; датный фильтр давал ошибочные 24 566 397 / 45 — golden берётся БЕЗ дат.)_

### 2. Заявки/обращения не потеряны
- [ ] Заявка-сторона: Σ обращения/korr/квал/priezd/продажи по ВСЕМ направлениям ДО==ПОСЛЕ (инвариант).
- [ ] Визит-сторона: старое не пропало (июнь Контекст 73 → Контекст ~68 + SEO ~5 = ~73); прирост = SEO/SEO Flow/пиксель(~15)/посевы(8), не шум.

### 3. Звонки не потеряны
- [ ] Число строк `_source_table='calls'` ДО==ПОСЛЕ; Σ обращения/продажи по звонкам сохранены при перекраске.

### 4. Нет задвоения
- [ ] Заявка-сторона: у каждого звонка/лида ровно ОДНО направление (SET, не INSERT).
- [ ] Визит-сторона: ни один звонок не попал ОДНОВРЕМЕННО в ветку «Авто-звонки» и «посевы» (step13).

### 5. Звонки→посевы
- [ ] `3_false_positive` (116/3015/39) → ~0; `1_by_design` (7/304/2) сохранены.
- [ ] Разбивка 116 доменов по каналам (Контекст/SEO); нет доменов без статуса.
- [ ] Будущие месяцы без закупки больше не красятся посевами.

### 6. Воронка
- [ ] korr ≥ kval ≥ priezd ≥ prodazhi на перекрашенных звонках.

### 7. PBI
- [ ] Пересобрать star; пользователь refresh; атрибуция стабильна, новые направления видны на визите.

### 8. Салон «Нева Авто» — продажи не потеряны (регресс-проверка)
- [ ] Пользователь: по салону «Нева Авто» БЫЛО гораздо больше продаж, сейчас всего 2 (Май 1 + Июнь 1, при 123 обращениях / 56 квал). Проверить ДО (исторически/ожидаемо) vs ПОСЛЕ.
- [ ] Найти, КУДА делись продажи Нева Авто: потеря строк / перекраска направления (звонки→посевы) / визит-сторона (Var A) / проблема матчинга приездов-продаж с CRM.
- [ ] Проверить в ОБЕИХ моделях атрибуции (по заявке и по визиту) — продажи Нева Авто не потеряны ни там, ни там.
- [ ] Если реальная потеря (а не перераспределение/десинк) — локализовать причину в коде (step3/step6/step13) и предложить фикс. Если артефакт сегодняшнего рассинхрона — подтвердить, что на чистом прогоне восстановилось.
---
