# План оптимизации веса таблиц `ad_analytics` (BA6)

_Составлен 2026-08-14. Фаза 0 (замер + пробы кодеков) выполнена в тот же день — все цифры ниже
измерены на живом ClickHouse `rc1b-q7j2ie10fdverqrk`, а не оценены._

## Цель и границы

**Оптимизируем только СВОИ таблицы** в `ad_analytics`, опираясь на выгрузки, которые уже лежат в
`raw_data`. `raw_data` не трогаем: её пишут внешние загрузчики (`raw_rows` 03:01,
`yandex_direct_report_*` 12:42), владелец вне этого репозитория.

Все целевые таблицы пересобираются каждым прогоном через CTAS + shadow/swap, поэтому **весь план —
это правки DDL в коде сборки, без `ALTER` на живых таблицах и без миграции данных.**

### Baseline (снимок 2026-08-14 21:19)

`logs/table_weight_ad_analytics_20260814_211915.json`, снят `tools/table_weight_report.py`.

| Слой | Размер |
|---|---:|
| `ad_analytics` всего (53 таблицы с данными) | **2 449 МиБ** |
| − `yd_search_query_report_master` (проект `work/yd_SEARCH_QUERY_REPORT`, BA6 не читает) | 819 МиБ |
| − `yandex_direct_cookie_analytics_website_pages` (отдельный сервис) | 33 МиБ |
| **= наши таблицы BA6** | **≈ 1 597 МиБ** |

Прогноз по измеренным коэффициентам: **−750 МиБ, это ≈ 47% нашего слоя.**

---

## Ход выполнения

| Фаза | Статус | Факт |
|---|---|---|
| 0 | ✅ 2026-08-14 21:19 | снимок `logs/table_weight_ad_analytics_20260814_212018.json`, кодеки выбраны тремя пробами |
| 1 | ✅ 2026-08-14 21:43 | `--only-step=1` 868.2 с; `raw_yandex` **611.14 → 225.64 МиБ (−385.49, −63.1%)**, `local_leads_all_pg` дропнут (−53.49 МиБ). **Итого −439 МиБ** |
| 2 | 🚚 код готов, задеплоен, ждёт полного прогона | 137 тестов зелёные, `py_compile` OK |
| 3 | ⏸ ждёт ручного шага в Power BI Desktop | связь факта с `bi_Dim_PlacementFeed` + публикация датасета |

**Сверка данных фазы 1 (нулевая дельта):** помесячные чексуммы `raw_yandex` по всем 20 колонкам
(`cityHash64` от полной строки) за 202601-202607 **совпали бит-в-бит** с baseline, снятым до
пересборки; строки и `sum(total_cost)` тех же месяцев тоже совпали. Разошёлся только открытый
202608: 1 455 571 → 1 576 119 строк (+120 548) — это свежие данные источника, залитые в
`raw_data.yandex_direct_report_rows` в 12:42. Итоговые 25 958 863 строки `raw_yandex` в точности
равны числу строк источника.

---

## Фаза 0 — ВЫПОЛНЕНА 2026-08-14

1. `tools/table_weight_report.py` — снимок веса (таблицы из `system.parts`, колонки из
   `system.parts_columns`; у мелких таблиц формата Compact разбивки по колонкам нет, итог верный).
   `--compare <old.json>` печатает дельту «было → стало». Тест: `tests/test_table_weight_report.py`.
2. Проба на партиции `202607` таблицы `raw_yandex` (3 677 890 строк, контроль 88.82 МиБ):

   | Вариант | Размер | Дельта |
   |---|---:|---:|
   | как есть | 88.82 МиБ | — |
   | + `LowCardinality` на 11 строковых | 76.78 МиБ | −13.5% |
   | + кодеки на числовых | 72.40 МиБ | −18.5% |
   | **всё вместе, без `id`** | **32.21 МиБ** | **−63.7%** |

3. Проба на партиции `202607` таблицы `fact_region_spend` (2 139 558 строк, контроль 27.07 МиБ):

   | Вариант | Размер | Дельта |
   |---|---:|---:|
   | как есть | 27.07 МиБ | — |
   | `T64+ZSTD(3)` на `Decimal(18,6)` | 19.24 МиБ | −28.9% |
   | то же + `id_location`→`Int32` | 19.20 МиБ | −29.1% |
   | **`ZSTD(3)` на `Decimal(18,6)`** | **17.74 МиБ** | **−34.5%** |

4. Проба кодеков на колонке расхода:

   | Колонка | none | `T64+ZSTD(3)` | `ZSTD(3)` | `Delta+ZSTD(3)` | `Gorilla+ZSTD(3)` |
   |---|---:|---:|---:|---:|---:|
   | `raw_yandex.total_cost Decimal(18,9)` | 20.02 | **15.06** | 16.09 | 17.18 | — |
   | `pbi_import.total_cost Float64` | 9.32 | — | **7.86** | — | 12.12 |

**Выводы, которые меняют план:**

- Единого «лучшего кодека» нет: для `Decimal(18,9)` в `raw_yandex` выигрывает `T64+ZSTD(3)`, для
  `Decimal(18,6)` в фактах — чистый `ZSTD(3)`, для `Float64` — `ZSTD(3)`, `Gorilla` хуже, чем без
  кодека вообще.
- **`id_location Nullable(Int64)` → `Int32` не даёт ничего** (−0.2 п.п. сверх кодека) — пункт
  выброшен из плана как лишний риск. Хватает кодека.
- Контроль в пробе 3 уже включал `T64` на `campaign_id`/`ad_group_id`, поэтому на живых таблицах
  выигрыш будет чуть больше измеренного, не меньше.

---

## Фаза 1 — мёртвый вес, риск нулевой (−251 МиБ, замерено)

| # | Что | Факт | Где править |
|---|---|---|---|
| 1.1 | `raw_yandex.id` — `cityHash64(row_key)`, случайный 64-бит хэш, не в `ORDER BY`, ни один потребитель не читает (grep по всем `*.py`/`*.sql` вне `archive/`) | **198 МиБ = 32% таблицы** | `step1_load_raw/step1.py:278` + строка `id,` во внешнем `SELECT` |
| 1.2 | `ad_analytics.local_leads_all_pg` — PG-фолбэк, читается только если пропала `raw_data.leads_all`; та живая (обновлена 14.08 03:25) | **53.4 МиБ** | `DROP TABLE`; код `step1.py:436-437` не трогать — он сам проверяет `table_exists()` |

**Проверка:** строки `raw_yandex` до/после идентичны; `verify_big_analytics.py` PASS; суммы
`cost/clicks/impressions` по `fact_big_analytics` без сдвига.

---

## Фаза 2 — типы и кодеки, смысл данных не меняется (−480 МиБ по измеренным коэффициентам)

### 2.1 `raw_yandex` 611 МиБ → ≈ 222 МиБ (−63.7%, измерено)

`LowCardinality` на 11 строковых колонках (замеренная кардинальность на 25.8 млн строк):
`Device` 4, `AdNetworkType` 2, `manager_login` 5, `account_login` 1 060, `CampaignName` 19 348,
`AdGroupName` 44 893, плюс `adgroup_code`, `campaign_code`, `tp`, `cpc_cpa`, `site_quiz`.

Кодеки: `total_cost Decimal(18,9)` → `CODEC(T64, ZSTD(3))`; `Impressions`, `Clicks`, `AdGroupId`,
`CampaignId`, `RlAdjustmentId` → `CODEC(T64, ZSTD(3))`; `key3 String` → `CODEC(ZSTD(3))`.

Имена кампаний/групп **остаются в таблице** — их читают 15 файлов (step3, step13, corrections,
build_star, utm_direct_audit…); `LowCardinality` даёт выигрыш без рефакторинга потребителей.

⚠️ `total_cost` не менять по scale и не приводить к int — правило проекта про дробную атрибуцию.
Экономия берётся кодеком.

Где: `step1_load_raw/step1.py:266-345` (`_raw_yandex_sql`) — нужен явный список колонок с типами
вместо вывода типов из `SELECT`.

### 2.2 Фактовые таблицы (−34.5% на каждой, измерено на `fact_region_spend`)

`ZSTD(3)` на всех `Decimal(18,6)`-метриках, `T64+ZSTD(3)` на `*_id`, `LowCardinality` на
`account_login` / `ad_network_type_key`.

| Таблица | Сейчас | Прогноз |
|---|---:|---:|
| `fact_region_spend` (`region_spend/build_region_spend.py:25`) | 179.9 МиБ | ≈ 118 |
| `fact_direct_feed_funnel_light` (`direct_feed_funnel/build.py:54`) | 180.4 МиБ | ≈ 118 |
| `fact_criterion_spend` (`criterion_spend/build_criterion_spend.py:30`) | 101.9 МиБ | ≈ 67 |
| `fact_big_analytics` (`star_refactor/build_star.py`) | 98.2 МиБ | ≈ 64 |
| `fact_adformat_spend` (`adformat_spend/`) | 51.9 МиБ | ≈ 34 |
| `raw_leads` (`step1_load_raw/step1.py`) | 55.7 МиБ | ≈ 39 |
| `fact_region_zayavki`, `fact_criterion_zayavki` | 17.2 МиБ | ≈ 11 |

### 2.3 `fact_direct_feed_funnel_light.placement_feed_key_hash`

`UInt64` от `cityHash64` (34 929 значений, 40.6 МиБ) стоит **в `ORDER BY`** — сортировка по шуму
портит сжатие и самого ключа, и соседних колонок. Заменить на плотный `UInt32`-суррогат
`feed_id`, присваиваемый при сборке `Dim_PlacementFeed`. Делать **после** 2.2, отдельным замером:
эффект тут не в самой колонке, а в порядке сортировки всей таблицы.

**Проверка фазы 2:** для каждой таблицы — сверка сумм всех метрик до/после с нулевой дельтой
(`cost`, `clicks`, `impressions`, вся воронка), `verify_big_analytics.py` PASS, `pytest tests/`
зелёный (`test_star_refactor_contracts.py` держит контракты звезды).

---

## Фаза 3 — не тащить в PBI то, что уже есть в справочнике (−85 МиБ + вес модели)

`pbi_import_fact_direct_feed_funnel` (209.7 МиБ, 13.2 млн строк) заново материализует ровно то, что
из светлого факта убрали месяц назад:

| Колонка | Вес | Действие |
|---|---:|---|
| `placement_feed_key String` (34 929 значений) | **72.2 МиБ** | отдать в PBI ключ `UInt32` + `bi_Dim_PlacementFeed` отдельной таблицей и связью в модели. Убирает 72 МиБ в CH и повторный словарь в VertiPaq |
| `total_cost/impressions/clicks Float64` | 116 МиБ | `CODEC(ZSTD(3))` → −15.7% (измерено) |
| `domain Nullable(String)` | 3.1 МиБ | резолвить из `Dim_Site` по `site_key` в модели |
| 12 колонок-констант `0` (`nekorr`, `kval`, `priezd`, `dobro`, `dohod_do_kredita`, `filtr`, `ne_otvechaet`, `nedozvon`, `priedet`, `goal_crm_order_canceled`, `goal_crm_spam_order`, `is_tp67`) | ~0 в CH | убрать из проекции: в CH бесплатны, но едут в модель PBI |
| 3 дубля (`goal_all_forms`=`kol_vo_zayavok`, `goal_crm_order_created`=`korr`, `goal_crm_order_paid`=`prodazhi`) | — | оставить одну версию |

⚠️ Перед удалением любой колонки прогнать `star_refactor/pbi_handoff/find_field_refs.py` — колонка,
на которую ссылается мера DAX, не удаляется.

Где: `star_refactor/build_pbi_compat.py:306-370`. Требует ручного шага Семёна в Power BI Desktop
(добавить связь и опубликовать датасет) — **до публикации фазу не деплоить.**

---

## Скорость прогона (отдельная задача, 2026-08-14)

Полный прогон занимал **130 минут**. Разбор по `data_quality_log` (`run_id=d68d74cb8465`):
step3 3234 с (41%), `direct_spend_staging` 900 с, step1 898 с, step11 640 с, `build_star` 536 с.

**Что нашлось:** инстанс ClickHouse — 2 vCPU / 8.33 ГБ (`CGroupMaxCPU=1.99`), а пайплайн ставил
`max_threads: 1`, то есть использовал одно ядро из двух. Плюс окно пакетной вставки было равно
одному дню: step3 из-за этого выполнял 1356 запросов (226 дней × 6 веток), step1 — 226.

**Замер на живой БД (партиция 202607), выбор ширины окна по факту:**

| Кейс | 1 поток | 2 потока |
|---|---:|---:|
| step1, день (119k строк) | 3.5 с | 3.1 с |
| step1, неделя (787k строк) | 6.7 с | 3.2 с |
| step1, месяц (3.68 млн строк) | 14.2 с | 11.4 с |
| step3 direct, день | 4.0 с | 1.0 с |
| step3 direct, неделя | 6.1 с | 8.1 с |
| step3 direct, месяц | `MEMORY_LIMIT_EXCEEDED` (241) | `MEMORY_LIMIT_EXCEEDED` (241) |

Неделя данных обрабатывалась почти за то же время, что один день — время съедали накладные расходы
на запрос. Месяц отпадает: step3 не влезает в память даже на лимите 1 ГБ.

**Что поставлено** (`config/ch_utils.py`): `max_threads` 1→2, `max_memory_usage` 512 МБ→1 ГБ,
пороги спилла 128→512 МБ, `max_execution_time` 60→300 с, ширина окна день→неделя через
`PIPELINE_BATCH_DAYS` (226 окон → 33, разом для всех 18 мест в 12 файлах, звавших `day_ranges`).

Ширина окна на результат не влияет: все 18 мест фильтруют `WHERE Date >= lo AND Date < hi` и не
пишут границу окна в данные; стык окон держит тест `tests/test_batch_windows.py`.
**Аварийный откат без правки кода:** `PIPELINE_BATCH_DAYS=1 python3 pipeline.py`.

**Факт после правок:** прогон целиком **130 → 31.5 мин** (сумма `duration_sec` всех шагов:
7809 → 1891 с). Крупнейшее: step3 3234 → 424 с, `direct_spend_staging` 900 → 136 с,
step1 868 → 203 с, step11 640 → 113 с, `build_star` 536 → 236 с, step6 390 → 82 с.

### ⚠️ Регресс, который дало расширение окна, и его фикс

Приёмка `director` поймала то, что не ловит golden: **`step11` считает веса пикселя за месяц,
взятый из первого дня окна (`toStartOfMonth(lo)`), а пиксельные дни тянет за всё окно.** Пока окно
было равно дню, оба всегда попадали в один месяц. На недельных окнах 7 окон пересекли границу
месяца → дни «с той стороны» не нашли весов в INNER JOIN и упали в ветку `CampaignId=0` без
разделения: **24 дня, 18 717 заявок (11.1%), 148 продаж (11.9%), 14.07 млн ₽ (11.0%)**.
Тоталы при этом сохранялись — потому golden и дал PASS.

**Фикс — в корне, а не в step11:** `range_batches` прибивает край окна к первому числу месяца,
поэтому окно физически не может пересечь границу месяца. Это закрывает и все будущие шаги с
месячным агрегатом внутри батча. Держит тест `test_no_window_crosses_a_month_boundary`.
Окон стало 36 вместо 33.

**Операционный факт (стоил одного упавшего запуска):** `--from-step=` выше step3 после успешного
прогона невозможен — шаг 148 `cleanup_wide_intermediates` штатно удаляет широкие промежуточные
таблицы, и step11 падает на `UNKNOWN_TABLE: ad_analytics.big_analytics_sources`. Перепрогон после
правки любого пост-step3 шага = полный прогон (сейчас это 31 минута).

---

## Что осознанно НЕ делаем

- **Чужие таблицы не трогаем вообще** (Семён, 2026-08-14) — ни правкой, ни тикетом:
  `yd_search_query_report_master` (819 МиБ, проект `yd_SEARCH_QUERY_REPORT`),
  `yandex_direct_cookie_analytics_website_pages` (33 МиБ, отдельный сервис), вся схема `raw_data`
  (внешние загрузчики). Периметр плана — только таблицы, которые собирает пайплайн BA6.

- **`raw_yandex.key3 String` → хэш** (55 МиБ): ключ участвует в anti-join step3 и в матчинге
  `key3_arrival_date` step13 — цена рефакторинга выше экономии. Берём с него `ZSTD(3)`.
- **`id_location` → `Int32`**: измерено, выигрыша нет.
- **Замена `pbi_import_*` физики на VIEW**: в CH view бесплатна по хранению, но джойн
  пересчитывается на каждый refresh — её ради этого и материализовали.
- **Вынос `CampaignName`/`AdGroupName` в `Dim_*`**: 15 файлов-потребителей, отдельный рефакторинг.
- **`raw_data`**: чужая зона, внешние загрузчики.
- **Incremental refresh** — запрещён правилом проекта, к весу отношения не имеет.

---

## Порядок исполнения и гейты

Маршрут: правки — исполнитель, приёмка — `director` (исполнитель себя не принимает), деплой —
контракт `/deploy-victory` (scp → md5 → `py_compile` → прогон).

| Шаг | Прогон | Гейт |
|---|---|---|
| Фаза 0 | нет | ✅ снимок «до» в `logs/`, кодеки выбраны по факту |
| Фаза 1 | `--only-step=1` | golden PASS, строки идентичны |
| Фаза 2 | полный (~130 мин) | нулевая дельта по всем метрикам, `pytest` зелёный, golden PASS |
| Фаза 3 | `--only-step=146/147` | `find_field_refs.py` чист, refresh PBI не длиннее baseline, вкладка «Фиды» отдаёт те же числа |

Фазы 1 и 2 сводятся в один полный прогон, чтобы не платить 130 минут дважды.

**Откат:** каждая фаза — отдельный коммит с правкой DDL в коде; откат = `git revert` + пересборка
затронутого шага. Живые данные не мигрируются, старые таблицы не удаляются до успешного swap.

**Критерий «готово»:** `tools/table_weight_report.py --compare` показывает ≥ 500 МиБ экономии,
`verify_big_analytics.py` PASS, все PBI-вкладки отдают числа baseline.
