# GOLDEN BASELINE — эталонные значения для проверки данных

> Эти числа — **golden-oracle** для верификации `big_analytics_v5`.
> При ЛЮБЫХ изменениях в pipeline / атрибуции / воронке / star-схеме —
> прогнать проверку против этих значений. Расхождение = ошибка в данных, разбираться.

---

## Эталон: Кудерко

| Метрика | Значение | Допуск |
|---------|----------|--------|
| Расход | **25 422 774.00** | **±100.00 ₽** (пиксельный дрейф, расширен 2026-06-17, см. ниже) |
| Продажи | **≥ 54** (floor) | рост сверху ОК; падение ниже 54 = FAIL |
| Обращения (заявки) | ~5069 (дрейф ±, см. п.4) | единицы (UPSERT) |
| Квалы | **~677 (category-kval, дрейф ±)** | единицы (UPSERT) |
| Визиты | ~575 (дрейф ±) | единицы (UPSERT) |

> ✅ **Квалы = категория `qualified` (category-kval) — это КОРРЕКТНАЯ формула, не регрессия.**
> `kval` считается прямо из категории `qualified` в `local_crm_statuses` (см. [`FUNNEL.md`](FUNNEL.md) L53),
> так со времён рефакторинга воронки (май 2026). Прежний ориентир ~1752 относился к
> **старой** формуле `korr − ne_otvechaet − filtr − nedozvon` и БОЛЬШЕ НЕ актуален —
> category-kval даёт **677** (факт read-only 2026-07-15, специалист Кудерко, срез golden).
> Значение стабильно (677 подтверждён и с ordering-race-фиксом, и без него, и на чистом
> revert+rerun — kval НЕ зависит от step3-фикса посевов). Дрейфует на единицы по UPSERT
> CRM, как обращения/визиты. Это НЕ баг и НЕ повод для разбора — see history ниже (2026-07-15).

> ⚠️ **Жёсткий инвариант — расход `25 422 774.00 ± 100.00 ₽`.** Продажи — FLOOR ≥ 54.
> Эталон расхода **`.00`** (уточнено 2026-06-11 фактом из `public.fact_big_analytics`;
> прежнее `.03` — историческое, до пересборок 10–11.06).
> **2026-06-17: продажи floor поднят 47→54.** Факт 54 подтверждён на срезе без фильтра
> по Date. Механика — дозревание CRM (by-design, не задвоение): CRM-лиды дозрели между
> прогонами 14.06 05:51 (47 продаж) и 14.06 22:20 (53 продажи, step0 other=936к UPSERT).
> К 16–17.06 добавился +1 звонок 09.06 → итого 54 без датного фильтра.
> Проверки: fid-пересечение calls∩direct = 0, инвариант 5408 ≥ 1983 ≥ 651 ≥ 54 соблюдён.
>
> **🔧 Допуск по расходу ±100 ₽ (расширен 2026-06-17; было ±15 ₽ ранее 2026-06-17, ±10 ₽ с 2026-06-16, ±3 ₽ с 2026-06-11).**
> Пиксельный дрейф растёт с числом дробных строк step11: факт наблюдался до +24 ₽ — это by-design, не регресс.
> REF остался `25 422 774.00`. Пользователь подтвердил: дрейф +24 ₽ — погрешность, не ошибка.
> Причина — **накопление субкопеечных остатков дробной пиксельной атрибуции**: у Кудерко
> ~66 503 дробных строк, каждая несёт долю с округлением, суммарный дрейф — рубли.
> Это **НЕ задвоение и НЕ регрессия**, а by-design следствие дробной атрибуции (которую
> запрещено приводить к int). Golden-гейт по расходу проверяет `|факт − 25 422 774.00| ≤ 100.00 ₽`.
> История: ±3 ₽ (2026-06-11) → ±10 ₽ (2026-06-16) → ±15 ₽ (2026-06-17) → ±100 ₽ (2026-06-17).
>
> **Продажи — FLOOR ≥ 54 (обновлено 2026-06-17; было ≥47 с 2026-06-15).** Продажи —
> **растущая метрика**: лиды дозревают в CRM асинхронно (47 → 53 → 54).
> Рост продаж сверху — ожидаемое поведение (дозревание), НЕ должен давать FAIL.
> Падение ниже 54 — регрессия данных (потеря лидов/звонков Кудерко) → FAIL + разбор.
> Реализация: `verify_big_analytics.py` (`GOLDEN_SALES_FLOOR`), проверка `>= FLOOR`.
>
> Дрейф > 10 ₽ по расходу = 🛑 разбор. Продажи < 54 = 🛑 разбор.
> Реализация расхода: `verify_big_analytics.py` (`GOLDEN_COST_TOL`), golden_kuderko-блоки в
> `pipeline.py` / `fast_pipeline.py` (`_G_RASHOD_TOL`).
> Воронка (обращения/квалы/визиты) дрейфует на единицы из-за асинхронного UPSERT CRM-лидов (п.4).

**Условия среза:**
- Атрибуция: **по дате заявки** (date of lead / обращения)
- Директолог / специалист: **Кудерко** (точно `'Кудерко Семен'`)
- Источники: `('direct','tp8','tp9','tp10','seo','calls','direct_unmatched','direct_zero')` — **БЕЗ диапазона дат**
  (эталон 47/25 422 774.00 достигается без фильтра по `"Date"`; добавление диапазона
  `01.01–06.04` ОШИБОЧНО даёт 24 566 397/45 — **не фильтровать golden по датам**).

**Правило:** при внесении любых изменений ВСЕГДА сверяться с этими числами,
чтобы не было ошибок в данных. Если хоть одна метрика разошлась — стоп, разбор причины.

---

## 🔬 КАК ПРАВИЛЬНО ПРОВЕРЯТЬ ДАННЫЕ (методика, проверено 2026-06-07)

Эти детали критичны — без них легко свериться не с той витриной/колонкой и получить
ложное расхождение. Проверено сверкой: расход и продажи сошлись **до копейки**.

### 1. Витрина — `public.fact_big_analytics` (лёгкий факт), source = `big_analytics_unified`

Эталон сверяется по **`public.fact_big_analytics`** — лёгкий факт звезды (схема **`public`**,
не `star` — звезда консолидирована в public 2026-06-10). Он строится как проекция
`public.big_analytics_unified`, которая объединяет две атрибуции:
- `атрибуция = 'По дате заявки'` ← источник `big_analytics_full`
- `атрибуция = 'По дате визита'` ← источник `big_analytics_full_arrival`

Эталон снят при **`атрибуция = 'По дате заявки'`** (фильтровать по этой колонке всегда).
Сверка по `public.big_analytics_unified` с тем же фильтром даёт идентичный расход/продажи
(25 422 774.00 / 47) — обе таблицы валидны для golden.

### 2. Колонка специалиста — `"специалист"`, значение `'Кудерко Семен'`

НЕ `директолог`. Точное написание в данных — **`Кудерко Семен`** (с пробелом, как ФИО).
Перед сверкой по новому имени всегда брать точное написание через `DISTINCT "специалист"`.

### 3. Фильтр источников — расход только из Я.Директ

Метрики воронки и расход считаются по источникам:
`('direct','tp8','tp9','tp10','seo','calls','direct_unmatched','direct_zero')`.

⚠️ **Пиксель (`пиксель`, `пиксель_атрибуц`) в эталон НЕ входит.** Если его включить —
обращения улетают за 6800, расход за 27 млн. Расход = только Я.Директ (`direct + tp8/tp9/tp10`).

### 4. Атрибуция дробная — суммировать, НЕ приводить к int

Обращения/квалы/визиты/продажи — это `SUM(...)` дробных пиксельных долей, округлённый
в самом конце через `ROUND(...)`. НИКОГДА не усекать доли до int по строкам
(усечение = главный исторический баг проекта). Округление — только у итоговой суммы.

### 5. Дата — golden НЕ фильтруется по диапазону `"Date"`

⚠️ Эталон 25 422 774.00 / 47 достигается **БЕЗ** фильтра по `"Date"`. Добавление диапазона
`'2026-01-01'..'2026-06-04'` ОШИБОЧНО даёт 24 566 397 / 45. Не добавлять датный фильтр в golden.

### Эталонный SQL сверки

```sql
SELECT
    ROUND(SUM(total_cost)::numeric, 2)  AS rashod,
    ROUND(SUM(kol_vo_zayavok))          AS obrashcheniya,
    ROUND(SUM(kval))                    AS kvaly,
    ROUND(SUM(priezd))                  AS vizity,
    ROUND(SUM(prodazhi))                AS prodazhi
FROM public.fact_big_analytics          -- (или public.big_analytics_unified — идентично по расходу/продажам)
WHERE "специалист" = 'Кудерко Семен'
  AND "атрибуция" = 'По дате заявки'
  AND _source_table IN ('direct','tp8','tp9','tp10','seo','calls','direct_unmatched','direct_zero');
```

Подключение: MCP `postgres-victory` (read-only, с мака) или `ssh victory '~/venv/bin/python3 ~/pgq.py "..."'`.
⛔ `psql` на Victory сломан — не использовать.

### Порядок проверки

1. Прогнать SQL выше → получить факт.
2. Сравнить с эталоном: таблица `Метрика | Эталон | Факт | Δ | Совпало`.
3. **Расход и продажи обязаны совпасть точно** — если разошлись, срез/логика сломаны, разбор.
4. **Допуск на дрейф свежести:** обращения/квалы/визиты могут отличаться на единицы
   (порядка ±20 / ±2 / ±2), если `pipeline.py` давно не гонялся: CRM-лиды доезжают
   асинхронно через UPSERT в `local_leads_all`, а `big_analytics_full` пересобирается
   только при следующем прогоне pipeline. Это НЕ баг. Точное совпадение воронки —
   только сразу после свежего прогона `pipeline.py`.
5. Большое расхождение по расходу/продажам или >±несколько единиц по воронке без
   причины → стоп, искать ошибку в данных/логике.

### Сверка от 2026-06-11 (на состоянии БД после пересборки 10–11.06, `public.fact_big_analytics`)

| Метрика | Эталон (инвариант) | Факт | Δ | Вердикт |
|---------|--------------------|------|-----|---------|
| Расход | **25 422 774.00 ±10 ₽** | 25 422 775.83 | +1.83 | ✅ в допуске |
| Продажи | **≥ 54** (floor) | 47 | — | ⚠️ история (до обновления floor 2026-06-17) |
| Обращения | ~5069 | 5548 | +дрейф (свежий прогон, CRM-лиды доехали) | — |
| Квалы | ~1752 | 1989 | +дрейф | — |
| Визиты | ~575 | 596 | +дрейф | — |

Расход +1.83 ₽ — субкопеечный пиксельный дрейф (66 503 дробных строк), **в пределах допуска ±10 ₽**,
НЕ регрессия. Продажи ≥ 47 (floor OK). Воронка плавает по UPSERT (см. п.4) — норма.
_(Сразу после пересборки расход может быть ровно `.00`; между прогонами накапливается дрейф
до ~+6.89 ₽ (2026-06-16) — поэтому гейт сверяет с допуском ±10, а не на точное равенство.)_
**История фикса продаж 42→47:** недобор продаж был в звонках (`_source_table='calls'`) — звонки
получали специалиста по домену из gsheet, минуя rule1; фикс `step6._patch_kuderko_calls_specialist`
переназначает звонки переданных Кудерко аккаунтов (см. [`KNOWN_ISSUES.md`](KNOWN_ISSUES.md) #13).

### Наблюдение 2026-06-15 (дозревание CRM)

| Метрика | Эталон | Наблюдаемый факт | Вердикт |
|---------|--------|-----------------|---------|
| Расход | **25 422 774.00 ±10 ₽** | 25 422 775.83 | ✅ в допуске |
| Продажи | **≥ 47** (floor, исторический) | **53** | ✅ floor OK (рост — дозревание) |

Продажи выросли с 47 до 53 — норма: CRM-лиды дозревают асинхронно (статусы обновляются
в `local_leads_all` через UPSERT между прогонами pipeline). Это и есть причина перехода
с exact `== 47` на floor.

### Наблюдение 2026-06-17 (floor поднят 47→54)

| Метрика | Эталон | Наблюдаемый факт | Вердикт |
|---------|--------|-----------------|---------|
| Расход | **25 422 774.00 ±10 ₽** | 25 422 786.20 | ✅ в допуске (Δ+12.20 — допуск ±10 нарушен! см. ниже) |
| Продажи | **≥ 54** (floor) | **54** | ✅ floor OK |

Продажи выросли с 53 до 54: +1 звонок 09.06 (дата заявки за пределами 01.01–06.04).
Без фильтра по Date — 54. С фильтром 01.01–06.04 — 53.
Механика легитимна: дозревание CRM, задвоения нет (fid calls∩direct = 0), воронка соблюдена.
⚠️ Расход 25 422 786.20 (Δ+12.20) формально за пределами ±10 — пиксельный дрейф продолжает расти.
Если на следующем прогоне расход останется >10 ₽ — рассмотреть расширение допуска до ±15 ₽.

> _Прежняя сверка 2026-06-07 давала расход `.03` — историческое значение до пересборок 10–11.06;
> текущий инвариант `.00` подтверждён фактом из `public.fact_big_analytics` 2026-06-11._

---

## 🧰 Единый мастер-чекер — `data_check/verify_big_analytics.py`

«Дёрнул один скрипт — проверил всё». Единая точка запуска golden-проверок.

| Режим | Команда | Что проверяет |
|-------|---------|---------------|
| Быстрый гейт (default) | `python3 data_check/verify_big_analytics.py` | 10 блоков, только LOGGED-таблицы, без внешних API. Так зовут 3 пайплайна. |
| Мастер-прогон | `python3 data_check/verify_big_analytics.py --full` | гейт + операционка (Sheets: покрытие доменов, расход-без-лидов, воронка по проджектам, NULL-поля) + star-сверка (`public.fact_big_analytics` vs unified + ARP; схема консолидирована в public) |
| Без star | `... --full --no-star` | мастер без блока 12 |
| + Telegram | добавить `--tg` | отправка сводки |

**Exit code (контракт, не менять):** `0`=всё PASS · `1`=есть FAIL · `2`=crash.

**Блоки гейта (1–11):** golden Кудерко (расход ±100 / продажи floor≥54) · закон
сохранения расхода · дубли key3 · звонки∩посевы · пиксель-визит ≥90k · дробность
пикселя · инвариант воронки I3 · grand total продаж [3000;3700] · свежесть ·
**пиксель==pixel_score** (read-only эквивалент step11) ·
**пер-салонный инвариант воронки I_salon** (korr≥kval≥priezd≥prodazhi агрегатно
по каждому салону на `public.fact_big_analytics`, атрибуция 'По дате заявки';
эпсилон 1e-6 против субмикронных хвостов дробной атрибуции; добавлен 2026-06-15).

**Осознанно НЕ в гейте** (с причиной): step11 live-инвариант (источник
`big_analytics_pixel` транзиентен/пуст между прогонами) · step12 (не read-only:
TRUNCATE+INSERT + remote FDW) · `sales_attribution/verify.py` (другая подсистема).

### ‼️ ПРАВИЛО — новая golden/инвариант-проверка → СНАЧАЛА спросить

**При появлении ЛЮБОЙ новой golden/инвариант-проверки — СНАЧАЛА спросить
пользователя, добавлять ли её в мастер-чекер `verify_big_analytics.py`, и только
потом добавлять.** Не добавлять новые блоки молча. Дубль правила — в `CLAUDE.md`
этого проекта и в docstring `verify_big_analytics.py`.

---

_Записано: 2026-06-07. Источник эталона: указан пользователем как контрольный. Методика проверена сверкой с БД._
_Дополнено 2026-06-09: единый мастер-чекер verify_big_analytics + правило «спросить перед добавлением проверки»._
_Актуализировано 2026-06-11: расход-инвариант `.03`→`.00` (факт из `public.fact_big_analytics`),
golden без датного фильтра, витрина = `public.fact_big_analytics` (схема star консолидирована в public),
история фикса продаж 42→47 (звонки Кудерко)._
_Дополнено 2026-06-11: введён допуск по расходу **±3 ₽** (субкопеечный пиксельный дрейф дробной
атрибуции, ~66 503 дробных строк; факт ~25 422 775.83, Δ +1.83 ₽ — НЕ задвоение). Продажи — БЕЗ
допуска, ровно 47. Реализация: `GOLDEN_COST_TOL` в `verify_big_analytics.py`, `_G_RASHOD_TOL` в
`pipeline.py` / `fast_pipeline.py`._
_Расширен 2026-06-16: допуск по расходу **±3 ₽ → ±10 ₽** (REF 25 422 774.00 неизменён). Причина:
пиксельный дрейф растёт с числом дробных строк step11; текущий факт 25 422 780.89 (Δ+6.89 ₽) вышел
за прежний ±3 — это by-design, не регресс. ±10 ₽ при расходе 25.4М = 0.00004%. Реализация:
`GOLDEN_COST_TOL=Decimal('10.00')` в `verify_big_analytics.py`, `_G_RASHOD_TOL=10.00` в
`pipeline.py` / `fast_pipeline.py`._
_Расширен 2026-06-17 (ранее): допуск по расходу **±10 ₽ → ±15 ₽** (REF 25 422 774.00 неизменён). Причина:
пиксельный дрейф by-design продолжает расти: факт 25 422 786.20 (Δ+12.20 ₽) вышел за ±10.
Решение пользователя. Реализация: `GOLDEN_COST_TOL=Decimal('15.00')` в `verify_big_analytics.py`,
`_G_RASHOD_TOL=15.00` в `pipeline.py` / `fast_pipeline.py`._
_Расширен 2026-06-17 (позднее): допуск по расходу **±15 ₽ → ±100 ₽** для консистентности во всех трёх точках
(REF 25 422 774.00 неизменён). Причина: наблюдаемый дрейф +24 ₽ — пользователь подтвердил погрешность
пиксельной атрибуции. Реализация: `GOLDEN_COST_TOL=Decimal('100.00')` в `verify_big_analytics.py`,
`_G_RASHOD_TOL=100.00` в `pipeline.py` (L1429) и `fast_pipeline.py` (L1036); также убран датный фильтр
`BETWEEN 2026-01-01 AND 2026-06-04` из SQL fast_pipeline.py (был стейл с досинхронизации), `_G_PROD_REF`
поднят 47→54 в `fast_pipeline.py` (синхронизация с pipeline.py и verify). Деплой Victory: md5 Mac==Victory._
_Актуализировано 2026-06-15: продажи переведены с exact `== 47` на **floor `>= 47`** (растущая
метрика — дозревание CRM; наблюдаемый факт 53 на 2026-06-15; рост сверху не фейлит, падение <47 =
регрессия). Реализация: `GOLDEN_SALES_FLOOR` в `verify_big_analytics.py`. Добавлен блок 11
**пер-салонный инвариант воронки (I_salon)** в быстрый гейт — korr≥kval≥priezd≥prodazhi
агрегатно по каждому салону на `public.fact_big_analytics`; операционка сдвинута на блок 12,
star-сверка — на блок 13._
_Обновлено 2026-06-17: **floor продаж поднят 47→54** (факт 54 на срезе без фильтра по Date,
подтверждён read-only SQL; механика — дозревание CRM by-design: между прогонами 14.06 05:51 и
14.06 22:20 UPSERT ~936к CRM-строк добавил 6 продаж, к 17.06 +1 звонок 09.06 → итого 54;
задвоения нет: fid calls∩direct = 0; воронка 5408≥1983≥651≥54 соблюдена). Расход-эталон
25 422 774.00 и допуск ±10 не изменены. Правки: `verify_big_analytics.py` GOLDEN_SALES_FLOOR,
`pipeline.py` _G_PROD_REF, `fast_pipeline.py` _G_PROD_REF, `GOLDEN_BASELINE.md`, `CLAUDE.md`
(big_analytics_v5, корневой), `director.md`, `anton_sql.md`._
_Добавлено 2026-06-28: **блок 14 check_14_kval_cost** — мягкий инвариант стоимости квала
без пикселя. Формула: `SUM(total_cost)/SUM(kval)` по `fact_big_analytics` (атрибуция='По дате
заявки', `_source_table NOT IN ('пиксель','пиксель_атрибуц')`). SOFT-WARNING: `ok=True` всегда —
не валит exit code, виден в verify-отчёте и step8 Telegram. Маркер: `KVAL_COST_CHECK_2026-06-28`._
_**Пересмотрено 2026-07-15 (важно — семантика инвертирована):** category-kval (kval из
категории `qualified`) — это **КОРРЕКТНАЯ** формула (решение пользователя), не регрессия.
Стоимость квала при category-kval = **~20 913 ₽/квал** (факт read-only 2026-07-15:
cost=1 098 739 247.72 / kval=52 538). Норма: **[10 000; 30 000] ₽/квал** (расширена ещё
2026-07-02) — 20 913 лежит по центру диапазона. РЕГРЕССИЯ теперь = откат к старой
korr-negation формуле (`korr − ne_otvechaet − filtr − nedozvon`), при которой kval растёт,
а стоимость падает к ~8 870 ₽ и **пробивает нижнюю границу 10 000** → сигнал. Константы:
`KVAL_COST_LO=10_000`, `KVAL_COST_HI=30_000` в `verify_big_analytics.py`. (Прежний текст этой
строки ошибочно называл category-kval «регрессией к ~22 000 ₽» — это устарело и инвертировано.)_

_Добавлено 2026-06-18: **блок 12 check_12_marcar_gsheet_coverage** в быстрый гейт
`verify_big_analytics.py`. Проверяет агрегатную полноту `local_gsheet_priezdi_marcar`
(визиты+продажи с 2026-01-01) в `fact_big_analytics` (По дате заявки). Ключ связи:
domain из gsheet.source → local_gsheet_sites → canonical salon → fact_big_analytics.салон.
Домены без маппинга (victory_pxl, victory_vdl и т.п. — пиксельные коды вне Маркар-ETL)
исключены. Гейт (hard fail): mapped-домен с vizity>0 и priezd=0 в финале.
Продажи — WARNING в detail (by-design расхождение: gsheet считает по дате визита,
финал — по дате заявки + дробная атрибуция; TODO ужесточить после стабилизации).
Валидация на Victory (read-only, текущие данные): 20 mapped доменов, 20/20 PASS,
g_vizity=1342, f_priezd=22263 (финал больше — агрегирует больший диапазон),
g_prodazhi=182, f_prodazhi=1964. Маркер: MARCAR_GSHEET_COVERAGE_CHECK_2026-06-17.
Операционка (бывший блок 12) сдвинута на блок 13, star-сверка — на блок 14._

---

## История изменений эталона

_2026-07-15: **re-baseline квалов + переоценка блока 14 (решение пользователя).**_
- **Квалы: ориентир ~1752 → ~677.** Причина: `kval` считается из категории `qualified`
  (category-kval) — это ТЕКУЩИЙ и КОРРЕКТНЫЙ код (`config/status_sql.py`, со времён рефакторинга
  воронки май 2026, задокументировано в `FUNNEL.md` L53). Прежний ~1752 относился к старой
  korr-negation формуле и устарел. Расследование oleg_read_bd доказало: 677 стабилен и НЕ связан
  со step3 ordering-race фиксом (идентичен с фиксом, без него и на чистом revert+rerun).
  `config/status_sql.py` НЕ трогали — формула уже корректна; обновлены только эталон/пороги.
- **Факт read-only 2026-07-15 (срез golden Кудерко):** заявки=5567, korr=3219, kval=677,
  priezd=651, prodazhi=54, расход=25 422 798.00. Инвариант korr≥kval≥priezd≥prodazhi соблюдён.
- **Блок 14 (стоимость квала): семантика инвертирована.** category-kval даёт ~20 913 ₽/квал
  (глобально, cost=1 098 739 247.72 / kval=52 538) — это НОРМА, лежит по центру диапазона
  [10 000; 30 000]. Регрессия теперь = откат к korr-negation (~8 870 ₽, пробивает LO=10 000).
  Константы `KVAL_COST_LO/HI` (10 000/30 000) не менялись — уже корректно брекетят 20 913;
  переписан только framing-комментарий (называл category-kval «сломанной формулой»).
- **Задеплоено вместе со step3 ordering-race фиксом** (маркер ORDERING_RACE_FIX_2026-07-15),
  который был ошибочно откачен ранее 15.07 из-за неверной атрибуции kval=677 к нему.
  Файлы: `GOLDEN_BASELINE.md`, `verify_big_analytics.py`, `FUNNEL.md`, `CLAUDE.md`, `step3.py`.
