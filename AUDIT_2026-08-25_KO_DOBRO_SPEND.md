# Аудит 2026-08-25 — КО/добро и атрибуция расхода по директологам

> Отчёт по сессии: что проверяли, что нашли, что починили, что осталось.
> Отправная точка — два вопроса Семёна: правильно ли считаются `dohod_do_kredita` и `dobro`
> в финальных таблицах, и сходятся ли они с сервисом Adscope (`d.adscope.ru`).
> Adscope читает **ту же CRM-базу**, что и BA6 — расхождение не может объясняться источником.

---

## Состояние на конец сессии

| | |
|---|---|
| Коммиты | `854a37e`, `07575f1`, `9017fab`, `6c7e5b9` (+`4a4db20` STATE.md) |
| Victory | код **задеплоен**, бэкап `/home/semen_vi/ba6_v6_ch_pre_4commits_20260825_152214.tgz` |
| Прогон | **НЕ выполнялся** — остановлен на замере baseline |
| Приёмка | `07575f1` и `9017fab` — **needs rework**; `6c7e5b9` — accepted, но не виден в Power BI |
| ⚠️ Риск | крон `0 4 * * * cron_run.py` с `BA6_POWERBI_REFRESH=1` прогонит этот код сам |

---

## Часть 1 — КО (`dohod_do_kredita`) и добро (`dobro`)

### Как считалось до правки

`step3_build_sources/step3.py::_metric_expr()` матчил обе метрики по голой паре
`(crm, lower(reason))` против `reference_data.crm_status_mapping`, **отбрасывая `status` и `salon`**,
и не включал категорию `sale`.

### Найденные дефекты

| # | Дефект | Замер |
|---|---|---|
| 1.1 | Ключ без `status`/`salon` — одна и та же пара `(crm, reason)` имеет разные категории в зависимости от статуса | август: 203 лида КО и 106 добра засчитаны вопреки справочнику |
| 1.2 | Категория `sale` отсутствовала, хотя `FUNNEL.md` требует `sale ⊆ approved ⊆ credit` | продажи попадали в КО косвенно, через совпадение reason |
| 1.3 | КО оказывался **вне приездов** — нарушение вложенности воронки | июль 700 лидов, август 211 |
| 1.4 | КО > `korr` на витрине | 11 доменов (авг 1–24), 30 (июль 1–30) |
| 1.5 | `marcar`, `redauto`, `genzes` дают **структурный ноль** — нет ни одной reason-строки `credit`/`approved` | у Маркара одобрение живёт в статусе: «Дошел в КО», «Одобрение» — оба были помечены `visit` |
| 1.6 | `verify_big_analytics.py` не упоминал эти колонки вообще | golden-гейт регрессию не ловил |
| 1.7 | Код разошёлся с `FUNNEL.md` | док обещал auto-merge `sale`, SQL его не делал |

### Сверка с Adscope

Июль 1–30 (закрытый месяц, `leads_last_update=2026-07-30`), все домены:

| | КО | Добро |
|---|---:|---:|
| Adscope | 2 505 | 1 427 |
| BA5 (PostgreSQL) | 3 258 | 1 567 |
| BA6 (до правки) | 3 352 | 1 566 |

Обе версии BA считали одинаково и обе были выше Adscope — то есть дефект унаследован, а не внесён в v6.

Перебор вариантов на доменах, общих с Adscope (620 доменов, Adscope КО 2 480):

| вариант | КО | Δ | Σ\|Δ\| по доменам |
|---|---:|---:|---:|
| A — ключ `(crm, reason)` (как было) | 2 544 | +64 | 1 088 |
| B — полный ключ `credit+approved` | 1 840 | −640 | 814 |
| D — полный ключ, только `credit` | 1 029 | −1 451 | 1 493 |
| E — A минус покупки | 2 263 | −217 | 1 181 |
| **C — полный ключ `credit+approved+sale`** | **2 313** | **−167** | **643** |
| **C + маппинг Маркара** | **2 448** | **−32 (−1,3 %)** | **518** |

Гипотеза «в КО попадают покупки, их надо убрать» проверена и **не подтвердилась**: Adscope продажи
включает, вариант E ухудшает результат. Правильно — продажи включать, кроме наличных.

По CRM после варианта C + Маркар: `marcar` 0, `genzes` 0, `plex` +2, `mega` −9, `mauto` +21, `crmf` −44.

### Что сделано — `854a37e`, `9017fab`

- полный ключ `(crm, status, salon, reason)` через `_category_match_expr`
- наборы: КО = `credit+approved+sale`, добро = `approved+sale`
- `CASH_SALE_STATUSES` — наличные продажи `plex`/`genzes` исключены
- Маркар: `«Дошел в КО» → credit`, `«Одобрение» → approved` в `CODE_STATUS_CATEGORY`
- гард `_CODE_STATUS_SIDE_CATEGORIES` расширен
- `FUNNEL.md` приведён к коду
- в `verify_big_analytics.py` добавлена структурная проверка `dobro ≤ КО ≤ priezd ≤ korr`

Симуляция на `raw_leads` (проверено дважды — исполнителем и приёмкой независимо):

| | было | станет |
|---|---:|---:|
| КО, июль 1–30 | 4 622 | 4 422 |
| Добро, июль 1–30 | 2 094 | 2 406 |
| КО, август 1–24 | 3 102 | 3 275 |
| Добро, август 1–24 | 1 488 | 1 711 |
| КО вне приездов, июль | 700 | 0 |
| КО вне приездов, август | 211 | 0 |

Status-сторона (`korr/kval/priezd/prodazhi/Обращения`) — **побайтово идентична** до и после,
за оба периода. Приезды Маркара не двигаются: все 98 лидов остаются внутри visit-набора.

Поштучный разбор изменения (август): КО −214 ушло / +387 пришло = нетто **+173**;
добро −98 / +321 = **+223**. Июль: КО −721/+521 = **−200**, добро −158/+470.
(Ранняя оценка «203/106 лишних лидов» была неверна — считала только подмножество.)

### Осталось по части 1

| # | Что | Где |
|---|---|---|
| 1.A | ✅ Исправлено 2026-08-25: `lower()` на кириллице заменён на `lowerUTF8()` в reason/category matching | `step3.py::_category_match_expr`, `_metric_expr` |
| 1.B | Свести `full_last_day_incomplete=1` — второй активный FAIL гейта, к этим правкам отношения не имеет, но прогон завалит | `verify_big_analytics.py` |

---

## Часть 2 — атрибуция расхода по директологам

Отправная точка: на срезе Power BI за июнь у Кудерко висит **517 641,29 ₽**, хотя он в июне не активен.

### Как раскладывается спорная сумма

| домен | сумма | источник | кому принадлежит |
|---|---:|---|---|
| `stav-bucar.ru` | 500 333,40 ₽ | direct | Тумашенко Евгений |
| `stav-bucar.ru` | 15 600,00 ₽ | pixel | Тумашенко Евгений |
| `bucars-stav.ru` | 1 200,00 ₽ | pixel | Кудерко (верно) |
| `buauto54.ru` | 507,89 ₽ | direct | Кудерко (верно) |

### Найденные дефекты

| # | Дефект | Замер |
|---|---|---|
| 2.1 | 🔴 Недатированный JOIN по `login_key` **размножает строки** для 23 из 1 106 логинов, `GROUP BY` не схлопывает → `sum(cost)` задваивается | коэффициент ровно **2.0** на всех 23 логинах; **76,79 млн ₽** по истории, июнь **−5 692 484,15 ₽**, июль **−3 930 021,25 ₽** |
| 2.2 | 🔴 `Dim_Site` выбирает специалиста **большинством голосов по всей истории** домена | по `stav-bucar.ru`: Кудерко 11 584 строки (янв–апр) против Тумашенко 7 563 (фев–авг) → домен схлопывается в Кудерко |
| 2.3 | 🔴 `specialist_correction_expr` **не вызывался** в трёх расходных витринах и в `build_star.py` | `grep` даёт 0 совпадений; таблица правил есть в `corrections.py:99-159` с давних пор |
| 2.4 | Расход вне всех интервалов `launch_date`/`block_date` | июнь **20 584 028,43 ₽**, июль **18 818 551,60 ₽**; из них закрывается fallback'ом на единственную строку логина 18,99 / 17,74 млн, реально неразрешимо 1,59 / 1,08 млн |
| 2.5 | Мусор в справочнике | 5 299 строк, 3 272 с пустым `login_key`; **20 строк** с `block_date <= launch_date`; логин-заглушка с **417 строками** (`нет`, `авито`, `----`), 924 пересекающихся пары |
| 2.6 | `build_pbi_compat.py` не прокидывает `специалист` для трёх фактов | новая колонка до Power BI не доходит |
| 2.7 | Слайсер `специалист` в отчёте читает `Dim_Site`, а не датно-корректный факт | `relationships.tmdl`: `big_analytics_full.домен → Dim_Site.domain` |

### Механизм v5, который надо было повторить

`corrections.py:694-930` — переназначение директолога по **(фамилия, логин) + дата-барьер**:

```sql
UPDATE ... SET "специалист" = %s WHERE "Date" < %s AND account_login = ANY(%s)
```

Кудерко — 2026-04-10 / 65 логинов, Сергеев — 2026-04-21 / 9, Питеркина — 2026-06-19 / 28,
Чепелев — 2026-07-17 / 7. Барьер **строго исключающий**, нижней границы нет.

**Скоуп по списку логинов и есть защита от новых логинов.** Три августовских логина Кудерко
(`porg-x7wkhs7d` 20.08, `porg-vzw5t7mt` 18.08, `porg-ead45mqo` 14.08) в списке отсутствуют →
правило не срабатывает → они резолвятся через справочник как Кудерко. Отдельной логики не нужно.

В BA6 эта таблица **уже была** — `corrections.py:99-159`, байт-в-байт, заведена коммитом `a6f3f4b`
задолго до этой сессии. Вызывалась из заявочной оси, звонков, пикселя, визитной оси и fallback —
но **не из расходных витрин**.

### Что сделано — `07575f1`, `6c7e5b9`

- `spend/dated_site_join.py` — date-aware выбор строки справочника, приоритеты 1/2/3/99,
  портирован из `gs_best` v5; подключён всеми тремя билдерами
- `Dim_Site.специалист` → directory-first, majority-факт демонтирован в fallback
- `step13` пиксельный перенос специалиста
- `specialist_correction_expr` применён в трёх расходных витринах поверх `gs_best`

Проверено приёмкой независимо, сошлось до копейки:

| проверка | результат |
|---|---|
| `e-20086622` до барьера 10.04 | янв 66 269,36 / фев 786 223,45 / мар 905 723,81 / апр 258 892,18 → Кудерко |
| он же с 10.04 | 71 333,77 ₽ → Тумашенко (барьер исключающий) |
| августовские логины Кудерко | 174 502,59 / 50 510,19 / 103 811,73 ₽ — не тронуты |
| Кудерко по правилу | 15 268 947 ₽ |
| Сергеев | 9 521 378 ₽ |
| Чепелев | 2 865 117 ₽ |
| Питеркина | 9 450,52 ₽ — правило срабатывает **полностью**, но `gs_best` по её 28 логинам уже даёт её же; двигаются только 385 строк с пустым справочником |
| итоги расхода от `6c7e5b9` | **не двигаются** — подтверждено замером, не рассуждением |
| fallback-корзины | prio-3: 3 115 594 строк / 146 811 082,89 ₽; prio-99: 131 351 / 7 471 114,02 ₽ — ничего не выброшено |
| число строк помесячно | идентично до и после → дубли снимаются, строки не выпадают |

### Осталось по части 2

| # | Что | Почему важно |
|---|---|---|
| 2.A | ✅ Закрыто 2026-08-25: экран больше не должен брать расходного специалиста из бездатного `Dim_Site`; PBIP-визуалы переведены на `big_analytics_full.специалист`, spend-факты несут свой `специалист` | остаётся опубликовать PBIP/обновить датасет |
| 2.B | ✅ Исправлено 2026-08-25: повторный `specialist_correction_expr` в pixel-ветке step13 убран, остаётся `f.\`специалист\`` | `step13_arrival/step13.py` |
| 2.C | ✅ Закрыто 2026-08-25: `специалист` добавлен в `big_analytics_full.tmdl`, визуалы больше не ссылаются на `Dim_Site.специалист` | PBIP admin/user |
| 2.D | ✅ Закрыто 2026-08-25: `специалист` прокинут в `build_pbi_compat.py` для region/adformat/criterion spend views и добавлен в TMDL факты | `build_pbi_compat.py`, `fact_*_spend.tmdl` |
| 2.E | ✅ Исправлено в этом аудите: сумма задвоения зафиксирована как **76,79 млн ₽**; июньская и июльская дельты точны | док обновлён |
| 2.F | Санкция Семёна на деплой/прогон получена 2026-08-25; падение итогов трёх фактов на ~76,8 млн ₽ ожидаемо как снятие дублей | проверить после pipeline |
| 2.G | Консолидировать три инлайновые копии того же паттерна (`step3.py:1053`, `step3.py:1349`, `step6.py:129`); `pairs_sql` продублирован в трёх билдерах | модуль вынесли, источник не убрали |
| 2.H | Краевые правила в докстринге `dated_site_join.py` заявлены, но не описаны: 20 строк с пустым окном, поведение на логинах-заглушках. Нет контрактного теста на новую колонку | `_demo()` проверяет только форму строки |

---

## Часть 3 — доменная раскладка (открыто, `KNOWN_ISSUES #45`)

Adscope и BA6 читают одну базу, итог по лидам сходится, **поштучная раскладка по сайтам — нет**.

| | доменов | лидов |
|---|---:|---:|
| наши, с непустым `domain` | 672 | 56 511 |
| Adscope | 972 | 57 007 |
| | | Δ = −496 (−0,9 %) |

- на 620 общих доменах: у нас 50 994 против 56 209
- число лидов совпадает точно лишь на **155 доменах из 620**
- **81 824 из 138 335** июльских лидов (59 %) имеют `domain_id IS NULL`
- «лишние» 52 домена у нас (5 517 лидов) — площадки: `konvert.ru` 2 358, `victory-pixel.ru` 1 450,
  `avito.ru` 544, `perform.ru` 486, `drom.ru`, `auto.ru`

Гипотеза «Adscope берёт домен из `utm_source`» **проверена и опровергнута**: наивный fallback даёт
70 442 против 56 233 (+14 209), Σ|Δ| растёт с 7 589 до 20 937. На спорных доменах `utm_source` —
это `s:none`, `s:yandex.ru`, `yandex_aw`. Поле, по которому Adscope привязывает лид к сайту,
не найдено; кандидаты — `fid`, `campaign_id → сайт`, форма заявки.

**Пока это не решено, поддоменная сверка любой метрики воронки с Adscope недостижима.**

---

## Часть 4 — сравнение с BA5

| механизм | v5 | v6 до сессии | вывод |
|---|---|---|---|
| ключ reason-метрик | crm+salon+status на status-оси; на reason-оси **только общие строки**, CRM/салонные reason-строки в базе есть, но мертвы | голая пара `(crm, reason)` | v6 после правки **впереди v5** |
| `sale` в КО/добре | включён каскадом merge | не включён | v6 догнал |
| наличные продажи | **исключения нет**; попытка закомментирована 20.05.2026 | нет | v6-only |
| Маркар | не покрыт — намеренно исключён из `_CRM_TO_SOURCE_TYPE` | не покрыт | v6 впереди |
| расход → сайт, golden | **date-aware** `gs_best`, `step3.py:776`, приоритеты 1/2/3/99 | login-only до фикса 2026-08-25 | портировано в `spend/dated_site_join.py` |
| три расходные витрины | login-only, **в v5 тоже не чинили** | login-only | v6 впереди v5 |
| `Dim_Site` | статический выбор из справочника, без majority | majority по всей истории | регресс демонтирован в PBI-слое 2026-08-25: расходные визуалы должны читать fact-`специалист` |
| корректировка по (фамилия, логин, дата) | `corrections.py:694-930` | таблица есть, в расходных витринах не вызывалась | закрыто `6c7e5b9` |

Живая проверка: v5 отдаёт июнь по `stav-bucar.ru` **на 100 % Тумашенко**, утечки к Кудерко нет.

⚠️ Расхождение с v5 по КО/добру — **сознательное решение**, а не регрессия. Golden baseline определён
против цифр v5 по Кудерко, поэтому уход требует явного подтверждения Семёна.

---

## Baseline для проверки после прогона

| # | проверка | ожидание |
|---|---|---|
| 1 | домены `dohod_do_kredita > korr` в `pbi_big_analytics_full`, ось `домен`, `атрибуция='По дате заявки'`, `HAVING d>0` | авг 1–24: 11 → **0**; июль 1–30: 30 → **0** |
| 2 | `full_funnel_dohod_do_kredita_gt_priezd` в гейте | 4 618 → **0** |
| 3 | июнь Кудерко на спорном срезе | 517 641,29 → **1 707,89 ₽** |
| 4 | КО/добро, ось «По дате заявки» | июль ≈ 4 422 / 2 406, август ≈ 3 275 / 1 711 |
| 5 | итоги расхода по трём фактам | июнь **−5 692 484,15 ₽**, июль **−3 930 021,25 ₽** |
| 6 | `korr`, `kval`, `priezd`, `prodazhi`, `Обращения` | без изменений |
| 7 | `e-20086622` в расходных фактах | янв–апр → Кудерко, с мая → Тумашенко |
| 8 | `full_last_day_incomplete` | должен закрыться, иначе гейт красный |

---

## Что не проверено

- фактические значения в витринах после пересборки — вся симуляция шла на `raw_leads`/`raw_calls`
- цифры по ветке звонков в `FUNNEL.md` (июль КО 173→160, добро 96→100; авг 113→118, 81→84) —
  из отчёта исполнителя, read-only не воспроизводимы
- поштучные leaf-счётчики (−214/+387, −98/+321) — источник `_step3_leads_deduped` транзиентный,
  сейчас в БД отсутствует; провенанс односточниковый
- golden после прогона
